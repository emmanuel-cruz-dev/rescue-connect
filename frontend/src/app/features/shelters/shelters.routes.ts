import { Routes } from '@angular/router';

import { Shelters } from './pages/shelters';

export const SHELTERS_ROUTES: Routes = [
  {
    path: '',
    component: Shelters,
    title: 'Refugios asociados',
  },
];
